# Classe concreta para cálculo de rota de carro
class RotaDeCarro < RotaStrategy
    def calcular_rota(origem, destino)
      puts "Calculando rota de carro de #{origem} para #{destino}."
      # Lógica específica para calcular a rota de carro
    end
  end